/// Function that is being called when one column needs to be renamed
= Table.RenameColumns((<<previous step name>>),{{<<Old Column Name>>, <<New Column Name>>}})

/// Function that is being called when multiple columns needs to be renamed
= Table.RenameColumns((<<previous step name>>),{{<<Old Column Name1>>, <<New Column Name1>>},{<<Old Column Name2>>, <<New Column Name3>>},...})